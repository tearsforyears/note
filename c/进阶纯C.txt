结构体方法等
进阶的指针//值传递 引用传递 地址传递
多线程
可变长度参数
简单操作系统
简单编译器
简单矩阵库
# 细节
	c++可以使用默认参数
	cout<<i++<<i++<<i++; //从右往左执行
	逗号表达式的值等于最后一个表达式的值,但是执行的顺序是从左到右
	左值和右值
	i++和++i的实现
	//i++
	int temp;
	i+=1;
	return temp;
	//++i
	i+=1;
	return i;
	i++效率更高
	(++i)=7
	(i++)=7 //编译出错